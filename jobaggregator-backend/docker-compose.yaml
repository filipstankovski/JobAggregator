services:
  db:
    image: postgres:17.4
    restart: always
    environment:
      POSTGRES_USER: nvd
      POSTGRES_PASSWORD: nvd123
      POSTGRES_DB: nvd
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5434:5432"
volumes:
  pgdata:
